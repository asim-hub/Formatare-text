Asimionesei Daniel 311 CD 
 In functia int main() apelez functiile pe care le primesc ca parametrii in argv[1].Pentru fiecare functie in parte, eu stiu ce atribute trebuie sa primeasca , unele fiind obligatorii , iar altele fiind auxiliare.Daca atributele obligatorii lipsesc , voi ignora acea operatie ,iar daca atributele auxuliare nu sunt precizate le voi stabili eu in functie de operatia care urmeaza sa fie aplicata textului.Dupa gasirea unei operatii valide , functia corespunzatoare ei va fi imediat apelata si se va continua cautarea altor operatii valide daca exista.Acest lucru il fac printr-un while ( while(k<l) ).
Pentru caracterul 'w' sau 'W' apelez functia Wrap ,pentru caracterele 'C','R','L','J'/'c','r','l','j' apelez functiile corespunzatoare Center,Right,Left si Justify (pe acestea patru le-am grupat pentru ca au celasi antet si am vreut sa scurtez secventa de program care cauta si alepeaza functiile).Pentru 'P'/'p' apelez functia Paragraf, pentru 'i'/'I' apelez functia ListI , iar pentru 'O'/'o' apelez functia ListO.
Descriere functii:
 Wrap-> aceasta functie transcrie matricea de caractere primita ca parametru (original) intr-un vector (chr), apoi o rescrie in matricea original astfel in cat fiecare linie sa nu depaseasca max_line_length.Tot aici calculez lungimea celui mai mare cuvant ,iar in cazul in care aceasta lungime este mai mare ca max_line_length,afisez un mesaj de eroare.
 Center->Verific daca paramterii transmisi sunt corecti , iar in caz afirmativ aflu lungimea celei mai lungi linii(in functie de paritatea lungimii celei mai lungi linii si de paritatea lungimii liniei pe care doresc sa o centrez voi centa liniile).
 Left->aici verific daca parametrii trimisi sunt corecti, iar in caz afirmativ, pentru liniile din intervalul start_line si end_line verific daca incep cu spatii, iar daca da , le elimin pe toate.
 Right->aici alfu lungimea celei mai lungi linii din matrice (maxim), iar apoi pe fiecare linie in parte, care pe pozitia maxim-1 nu are caracterul '\n', iar pe pozitia maxim nu are '\0', o voi translata la dreapta cu cate o pozitie pana cand cele doua conditii sunt indeplinite.
 Justfy->aici aflu cea mai lunga linie, elimin spatiile de la inceputul fiecarii linii apoi parcurg fiecare linie din intervalul primit ca parametru, daca linia nu are lungimea maxima, adaug cate un spatiu intre fiecare doua cuvinte , dar acest lucru il fac consecutiv.Daca s-a ajuns cu adaugatul spatiilor la finalul liniei ,iar linia tot nu este aliniata la stanga si la dreapta cu cea mai lunga linie , voi continua sa adaug spatii de la inceputul liniei, imediat dupa primul cuvant.
 Paragraf->aici deplasesz la dreapta fiecare linie cu indent_length spatii.
 ListI->inaintea fiecarei linii pun mai intai cate un spatiu , apoi caracterul special , iar apoi numerotez linia , acest lucru il fac in funtie de parametrii primiti.
 ListO-> aici mai intai ordonez alfabetic liniile matricei din intervalul dat si apoi apoi aplic aceleasi operatii asupra textului ca la ListI.  
(In cadrul acestor functii mai efectuez operatii cum ar fi eliminarea de caractere albe (ex:spatii) de la inceputul liniilor maticei sau de la sfarsitul acestor linii , iar acest lucru depinde de functie).
